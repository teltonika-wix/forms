# Shared UI

New reusable primitives must be created in this module.
Do not add new primitives under `src/core`.

Current status:

- `src/shared/ui/index.ts` is currently a migration entrypoint placeholder.
- When introducing new shared primitives, add them here and export them from this module.
