# Shared

Reusable framework-agnostic and UI primitives belong here.

Current status:

- `src/shared` is the target home for shared modules introduced during migration.
- Existing reusable UI components are still primarily in `src/components`.
- Shared UI primitives should be exposed from `src/shared/ui`.
