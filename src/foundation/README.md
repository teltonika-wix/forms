# Foundation

Design tokens, base themes, and styling presets belong here.

Current status:

- This module is intentionally minimal during migration.
- Existing theme/preset code still lives in `src/base-themes` and `src/tailwind-configurations`.
- New cross-cutting design-system primitives should be added here first when practical.
